import styled from 'styled-components';

export const SectionLatestProjectsStyles = styled.section`
    padding-bottom: var(--spacingBig1);
    .buttonBar {
        margin-top: var(--spacingBig1);
        justify-content: flex-end;
    }
`;
